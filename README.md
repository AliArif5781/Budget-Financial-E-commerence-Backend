# Financial Web Application:

### Backend:

- Solve the issue of connection of MongoDB in project.

- Create a User Schema and DTO.

- Create a User type.

- Create a Signup controller and service and the user signup properly work, store the password in hash with bcrypt and the signup user cookies also store . test in (Bruno).

- User cannot signup with the same email.

- User can also login.

- If the email is not in database, and user try to login get error.

- Add rate limiting for prevent ddos and brute force attack.

- Implement AUTH Guard also use cookie parser and get the login user details.
